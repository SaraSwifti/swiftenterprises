import React from "react";
// { useEffect, useState }
// import Card from "@mui/material/Card";

function RenderReportsRusekView(){

return (
    <><h1> This is where the Rusek's report card feed will render where they can delete and edit</h1>
        <div className="card"> 
               <div>
                <h2> This is where the reports will render</h2>
                {/* <h2>{item.title}</h2>
                <h2>{item.date}</h2> */}
            </div>
        {/* <div className="mt-6 grid grid-cols-1 gap-y-10 gap-x-6 sm:grid-cols-2 lg:grid-cols-4 xl:gap-x-8">
          {reports.map((item) => (
            <div key={title.id} className="group relative">
              <div className="min-h-80 aspect-w-1 aspect-h-1 w-full overflow-hidden rounded-md bg-gray-200 group-hover:opacity-75 lg:aspect-none lg:h-80">
                <img
                  src={product.imageSrc}
                  alt={product.imageAlt}
                  className="h-full w-full object-cover object-center lg:h-full lg:w-full"
                />
              </div>
              <div className="mt-4 flex justify-between">
                <div>
                  <h3 className="text-sm text-gray-700">
                    <a href={product.href}>
                      <span aria-hidden="true" className="absolute inset-0" />
                      {product.name}
                    </a>
                  </h3>
                  <p className="mt-1 text-sm text-gray-500">{product.color}</p>
                </div>
                <p className="text-sm font-medium text-gray-900">{product.price}</p>
              </div>
            </div>
          ))}
        </div> */}
    {/* //     {reports.map(item => (
         

    //     ))

    // } */}


    </div>
    </>)
}

    export default RenderReportsRusekView;
